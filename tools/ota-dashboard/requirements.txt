# =========================================================
# AS AI — OTA & Diagnostics Dashboard
# ISOLATED dependency set. Install ONLY into tools/ota-dashboard/.venv
# NEVER into the repo-root .venv (that one belongs to PlatformIO).
# =========================================================
streamlit>=1.40,<2.0      # st.fragment(run_every=), st.dialog
boto3>=1.34,<2.0
botocore>=1.34,<2.0
pandas>=2.2,<3.0
tzdata>=2024.1            # zoneinfo database on Windows
